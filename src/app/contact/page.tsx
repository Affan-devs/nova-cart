import { NewsletterSection } from "@/src/components/home/newsteller";

export default function ContactPage() {
    return (
        <section>
            <NewsletterSection />
        </section>
    );
}